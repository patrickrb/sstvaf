name: Desktop GUI CI & Release

# Pipeline for the desktop/gui Tauri app (Rust + Vite/React). On PRs and
# non-release pushes it does a compile check (`tauri build --no-bundle`) across
# Windows + macOS + Linux. On main pushes and desktop-v* tags it builds native
# bundles on all three OSes and publishes them to a per-platform GitHub Release.
#
# Distinct from desktop/ci/windows-msi.yml, which packages the 32-bit C CLI —
# that lane owns the `v*` tags, this one owns `desktop-v*`, so the two never
# collide on the Releases page:
#   desktop-v* tag  -> production desktop GUI release
#   push to main    -> auto-bumped desktop-v* release
#   push to staging -> desktop-dev.<run_number> PRERELEASE
#   push to dev     -> compile-check only, NO release
# The dev build is cut on staging (a dev -> staging merge), never on dev itself.
#
# PATH-FILTERED: PRs and dev pushes only build when desktop/gui/** or the shared
# SSTV codec (sstvaf/app/src/main/cpp/sstv_lib/**) changes, or this workflow
# file. staging/main pushes and desktop-v* tags always build. The always-run
# `desktop-gui-gate` job is the required status check.

on:
  push:
    branches: [main, staging, dev]
    tags:
      - 'desktop-v*'
  pull_request:
    branches: [main, staging, dev]

permissions:
  contents: write

concurrency:
  group: >-
    ${{
      (github.event_name == 'push'
        && (github.ref == 'refs/heads/main'
          || github.ref == 'refs/heads/staging'
          || startsWith(github.ref, 'refs/tags/desktop-v')))
      && 'desktop-gui-release'
      || format('desktop-gui-{0}', github.run_id)
    }}
  cancel-in-progress: false

jobs:
  detect:
    name: Detect desktop GUI changes
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.decide.outputs.run }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Filter paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            desktop:
              - 'sstvaf/app/src/main/cpp/sstv_lib/**'
              - 'desktop/gui/**'
              - '.github/workflows/desktop-gui.yml'

      - name: Decide whether to run
        id: decide
        env:
          CHANGED: ${{ steps.filter.outputs.desktop }}
        run: |
          set -euo pipefail
          # staging/main pushes and desktop-v* tags always build the full
          # release; otherwise (dev push, PRs) build only when desktop-relevant
          # files changed.
          if [[ "$GITHUB_EVENT_NAME" == "push" && ( "$GITHUB_REF" == "refs/heads/main" || "$GITHUB_REF" == "refs/heads/staging" || "$GITHUB_REF" == refs/tags/desktop-v* ) ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          elif [[ "$CHANGED" == "true" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

  version:
    name: Determine desktop release tag
    needs: detect
    if: ${{ needs.detect.outputs.run == 'true' }}
    runs-on: ubuntu-latest
    outputs:
      release_tag: ${{ steps.tag.outputs.release_tag }}
      version_name: ${{ steps.tag.outputs.version_name }}
      should_release: ${{ steps.tag.outputs.should_release }}
      is_prerelease: ${{ steps.tag.outputs.is_prerelease }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fetch tags
        run: git fetch --tags --force

      - name: Determine release tag
        id: tag
        run: |
          set -euo pipefail
          # Mirrors the Android lane logic but in the desktop-v* / desktop-dev.N
          # namespace so desktop releases sort separately from the app's.
          if [[ "${GITHUB_REF}" == refs/tags/desktop-v* ]]; then
            echo "release_tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "version_name=${GITHUB_REF_NAME#desktop-v}" >> "$GITHUB_OUTPUT"
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          elif [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/main" ]]; then
            # Auto-bump the last component from the latest desktop-v* tag.
            latest=$(git tag --list 'desktop-v*' --sort=-v:refname | head -n1)
            if [[ -z "$latest" ]]; then
              new_tag="desktop-v0.1.0"
            else
              version="${latest#desktop-v}"
              IFS='.' read -ra parts <<< "$version"
              last_idx=$((${#parts[@]} - 1))
              parts[$last_idx]=$((parts[$last_idx] + 1))
              new_tag="desktop-v$(IFS=.; echo "${parts[*]}")"
            fi
            echo "Latest tag: ${latest:-<none>} -> new tag: $new_tag"
            echo "release_tag=$new_tag" >> "$GITHUB_OUTPUT"
            echo "version_name=${new_tag#desktop-v}" >> "$GITHUB_OUTPUT"
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          elif [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/staging" ]]; then
            # staging push (a dev -> staging merge) cuts the desktop dev build.
            new_tag="desktop-dev.${GITHUB_RUN_NUMBER}"
            latest=$(git tag --list 'desktop-v*' --sort=-v:refname | head -n1)
            base_ver="${latest:+${latest#desktop-v}}"
            base_ver="${base_ver:-0.1.0}"
            echo "release_tag=$new_tag" >> "$GITHUB_OUTPUT"
            echo "version_name=${base_ver}-dev.${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"
            echo "should_release=true" >> "$GITHUB_OUTPUT"
            echo "is_prerelease=true" >> "$GITHUB_OUTPUT"
          else
            # dev push, PR, or other branch — compile-check only, no release.
            echo "release_tag=" >> "$GITHUB_OUTPUT"
            echo "version_name=" >> "$GITHUB_OUTPUT"
            echo "should_release=false" >> "$GITHUB_OUTPUT"
            echo "is_prerelease=false" >> "$GITHUB_OUTPUT"
          fi

  build:
    name: Build (${{ matrix.os }})
    needs: [detect, version]
    if: ${{ needs.detect.outputs.run == 'true' }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: desktop/gui/src-tauri

      # Tauri v2 Linux system dependencies (webkit2gtk-4.1 + GTK + packaging),
      # plus the native libs our crates link against on Linux:
      #   - libudev-dev:    serialport's libudev-sys (CAT rig control)
      #   - libasound2-dev: alsa-sys (audio capture/playback)
      # Without these the Linux build fails resolving the `libudev` / `alsa`
      # pkg-config packages.
      #
      # ubuntu-latest is Ubuntu 24.04, which dropped libappindicator3-dev in
      # favour of the Ayatana fork — use libayatana-appindicator3-dev (Tauri's
      # current documented prerequisite).
      #
      # DEBIAN_FRONTEND=noninteractive + DPkg::Lock::Timeout guard against the
      # two ways this step hangs forever on hosted runners: a debconf prompt
      # that -y doesn't suppress, and apt blocking on the dpkg lock held by a
      # background apt-daily job (instead, wait up to 10 min then fail loudly).
      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update -o DPkg::Lock::Timeout=600
          sudo apt-get install -y --no-install-recommends \
            -o DPkg::Lock::Timeout=600 \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            libudev-dev \
            libasound2-dev \
            patchelf \
            file \
            build-essential \
            curl \
            wget

      - name: Install frontend dependencies
        working-directory: desktop/gui
        run: npm ci

      - name: Frontend unit tests
        working-directory: desktop/gui
        run: npm test

      # --- Non-release: compile check only (no bundles), fastest path. ---
      - name: Compile check (no bundle)
        if: ${{ needs.version.outputs.should_release != 'true' }}
        working-directory: desktop/gui
        run: npm run tauri build -- --no-bundle

      # --- Release: build native bundles and upload to the per-platform
      # release. tauri.conf.json has bundle.active=false (keeps local dev builds
      # bundle-free), so enable bundling for the release build via an inline
      # --config merge. Each OS produces its native targets (dmg/app, msi/nsis,
      # deb/appimage) from the base config's targets:"all". All matrix legs share
      # one tagName; tauri-action creates the release once and attaches to it. ---
      - name: Build & publish release bundles
        if: ${{ needs.version.outputs.should_release == 'true' }}
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: desktop/gui
          tagName: ${{ needs.version.outputs.release_tag }}
          releaseName: ${{ needs.version.outputs.release_tag }}
          releaseBody: 'SSTVAF desktop GUI build ${{ needs.version.outputs.version_name }}.'
          prerelease: ${{ needs.version.outputs.is_prerelease == 'true' }}
          args: --config '{"bundle":{"active":true}}'

  coverage:
    name: Rust coverage
    # Best-effort, NON-GATING desktop coverage: runs the src-tauri crate's tests
    # under cargo-llvm-cov and uploads to Codecov under the `desktop` flag
    # (declared in codecov.yml with carryforward). Path-gated via `detect` and
    # intentionally left OUT of `desktop-gui-gate` — a coverage flake must never
    # block a merge or a release. Linux-only.
    needs: detect
    if: ${{ needs.detect.outputs.run == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: desktop/gui/src-tauri

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Install Linux dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update -o DPkg::Lock::Timeout=600
          sudo apt-get install -y --no-install-recommends \
            -o DPkg::Lock::Timeout=600 \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            librsvg2-dev \
            libayatana-appindicator3-dev \
            libudev-dev \
            libasound2-dev

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # cargo llvm-cov runs `cargo test`, which compiles build.rs ->
      # tauri_build::build(). tauri.conf.json's frontendDist ("../dist") must
      # exist at compile time, and cargo — unlike `tauri build` — does NOT run
      # the configured beforeBuildCommand, so build the Vite frontend here
      # first. Without this the crate fails to compile on a missing dist.
      - name: Build frontend (produces desktop/gui/dist for tauri-build)
        working-directory: desktop/gui
        run: |
          npm ci
          npm run build

      - name: Run tests with coverage
        working-directory: desktop/gui/src-tauri
        run: cargo llvm-cov --lcov --output-path desktop.lcov

      - name: Upload desktop coverage to Codecov
        continue-on-error: true
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: desktop/gui/src-tauri/desktop.lcov
          flags: desktop

  desktop-gui-gate:
    name: desktop-gui-gate
    # Always-run aggregator: the single required status check for the desktop
    # GUI. Reports success when every build leg passed OR the build was
    # path-skipped, so branch protection never gets stuck "pending" on an
    # unrelated change. Depends on `detect` and `version` too: a failure in
    # either skips `build`, and a skip alone would otherwise be read as
    # "nothing to do" and pass the gate — so their results are checked directly.
    needs: [detect, version, build]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Verify desktop jobs succeeded or were skipped
        env:
          DETECT_RESULT: ${{ needs.detect.result }}
          VERSION_RESULT: ${{ needs.version.result }}
          BUILD_RESULT: ${{ needs.build.result }}
        run: |
          set -euo pipefail
          for r in "$DETECT_RESULT" "$VERSION_RESULT" "$BUILD_RESULT"; do
            if [[ "$r" == "failure" || "$r" == "cancelled" ]]; then
              echo "::error::A desktop GUI job did not pass (detect=$DETECT_RESULT version=$VERSION_RESULT build=$BUILD_RESULT)"
              exit 1
            fi
          done
          echo "Desktop GUI gate OK (detect=$DETECT_RESULT version=$VERSION_RESULT build=$BUILD_RESULT)."
